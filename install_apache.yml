---
- name: Apache server installed
  hosts: rhel
  become: true
  tasks:

    - name: Install Apache
      ansible.builtin.yum:
        name: httpd

    - name: Apache enabled and running
      ansible.builtin.service:
        name: httpd
        enabled: true
        state: started
